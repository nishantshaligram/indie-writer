{
  "name": "indie-writer",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "watch:css": "sass --watch assets/scss:assets/css --source-map",
    "build:css": "sass assets/scss:assets/css --style=compressed --no-source-map",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "sass": "^1.89.1"
  }
}
